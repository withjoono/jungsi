# 프로덕션 환경 변수 템플릿
# 사용법: 이 파일을 .env.production으로 복사하고 실제 값을 입력하세요
# cp env.production.template .env.production
# ⚠️  .env.production 파일은 Git에 커밋하지 마세요!

# 노드 환경
NODE_ENV=production

# 데이터베이스 설정 (Cloud SQL via Proxy)
DB_TYPE=postgres
DB_HOST=localhost  # Cloud SQL Proxy를 통해 연결 시
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-production-password-here
DB_NAME=geobukschool_prod

# 데이터베이스 동기화 (프로덕션에서는 반드시 false!)
DB_SYNCHRONIZE=false

# Cloud SQL 인스턴스 정보
CLOUD_SQL_CONNECTION_NAME=ts-back-nest-479305:asia-northeast3:geobuk-db

# JWT 설정
JWT_SECRET=your-jwt-secret-here
JWT_EXPIRES_IN=7d

# Firebase Admin SDK
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-firebase-client-email
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Google Cloud Storage
GCS_BUCKET_NAME=your-gcs-bucket-name

# 서버 설정
PORT=8080
HOST=0.0.0.0

# CORS
CORS_ORIGIN=https://your-production-domain.com

# Sentry (에러 트래킹)
SENTRY_DSN=your-sentry-dsn-here

# Redis (캐시)
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# 로깅
LOG_LEVEL=info
