name: Deploy Backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'jungsi-backend/**'
      - '.github/workflows/deploy-backend-cloudrun.yml'
  workflow_dispatch:  # 수동 실행 가능

env:
  PROJECT_ID: ts-back-nest-479305
  REGION: asia-northeast3
  SERVICE_NAME: jungsi-backend
  CLOUD_SQL_INSTANCE: geobuk-db

jobs:
  deploy:
    name: Deploy to Cloud Run
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./jungsi-backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: jungsi-backend/yarn.lock

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker

      - name: Build Docker image
        run: |
          docker build -t gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} .
          docker tag gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
                     gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest

      - name: Push Docker image to GCR
        run: |
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }}
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest

      - name: Run Database Migrations
        run: |
          echo "🔄 Running database migrations..."
          
          # Cloud SQL Proxy 다운로드
          wget https://dl.google.com/cloudsql/cloud_sql_proxy.linux.amd64 -O cloud-sql-proxy
          chmod +x cloud-sql-proxy
          
          # Cloud SQL Proxy 시작 (백그라운드)
          ./cloud-sql-proxy ${{ env.PROJECT_ID }}:${{ env.REGION }}:${{ env.CLOUD_SQL_INSTANCE }} --port 5432 &
          PROXY_PID=$!
          
          # Proxy 준비 대기
          sleep 10
          
          # Dependencies 설치
          yarn install --frozen-lockfile
          
          # 환경 변수 설정 및 마이그레이션 실행
          export NODE_ENV=production
          export DB_HOST=localhost
          export DB_PORT=5432
          export DB_USER=${{ secrets.DB_USER }}
          export DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          export DB_NAME=${{ secrets.DB_NAME }}
          
          # 마이그레이션 실행
          yarn typeorm:run || echo "⚠️ Migration failed, but continuing deployment"
          
          # Proxy 종료
          kill $PROXY_PID || true
          
          echo "✅ Migration step completed"

      - name: Delete existing Cloud Run service (if exists)
        run: |
          echo "🗑️ Checking for existing Cloud Run service..."

          if gcloud run services describe ${{ env.SERVICE_NAME }} --region ${{ env.REGION }} --quiet 2>/dev/null; then
            echo "Found existing service. Deleting to ensure clean deployment..."
            gcloud run services delete ${{ env.SERVICE_NAME }} \
              --region ${{ env.REGION }} \
              --quiet || echo "⚠️ Failed to delete, but continuing..."
            sleep 10
            echo "✅ Service deleted"
          else
            echo "ℹ️ No existing service found. Proceeding with fresh deployment."
          fi

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image gcr.io/${{ env.PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --memory 1Gi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 10 \
            --port 8080 \
            --add-cloudsql-instances ${{ env.PROJECT_ID }}:${{ env.REGION }}:${{ env.CLOUD_SQL_INSTANCE }} \
            --set-env-vars NODE_ENV=production,DB_HOST=/cloudsql/${{ env.PROJECT_ID }}:${{ env.REGION }}:${{ env.CLOUD_SQL_INSTANCE }},DB_PASSWORD=${{ secrets.DB_PASSWORD }},DB_NAME=${{ secrets.DB_NAME }},DB_USER=${{ secrets.DB_USER }},JWT_SECRET=${{ secrets.JWT_SECRET }},DB_PORT=5432,DB_TYPE=postgres \
            --quiet

      - name: Get Cloud Run URL
        id: get-url
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region ${{ env.REGION }} \
            --format 'value(status.url)')
          echo "url=$URL" >> $GITHUB_OUTPUT
          echo "🌐 Service URL: $URL"

      - name: Notify deployment success
        if: success()
        run: |
          echo "✅ Deployment successful!"
          echo "🌐 Backend URL: ${{ steps.get-url.outputs.url }}"
          echo "📊 Service: ${{ env.SERVICE_NAME }}"
          echo "🗄️  Database: ${{ env.CLOUD_SQL_INSTANCE }}"
          echo "🔄 Migrations: Executed"

      - name: Notify deployment failure
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Please check the logs above for details."
